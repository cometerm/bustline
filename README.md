```bash
# you need bun use either of these commands
npm install -g bun
powershell -c "irm bun.sh/install.ps1|iex"

# install dependencies
bun install

# start server
bun dev

# before pushing build first
bun run build
```
